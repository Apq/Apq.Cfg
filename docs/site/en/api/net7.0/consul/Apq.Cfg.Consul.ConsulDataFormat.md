### [Apq\.Cfg\.Consul](Apq.Cfg.Consul.md 'Apq\.Cfg\.Consul')

## ConsulDataFormat Enum

Consul 数据格式

```csharp
public enum ConsulDataFormat
```
### Fields

<a name='Apq.Cfg.Consul.ConsulDataFormat.KeyValue'></a>

`KeyValue` 0

每个 KV 键对应一个配置项

<a name='Apq.Cfg.Consul.ConsulDataFormat.Json'></a>

`Json` 1

单个 key 存储 JSON 格式的配置

<a name='Apq.Cfg.Consul.ConsulDataFormat.Yaml'></a>

`Yaml` 2

单个 key 存储 YAML 格式的配置